import React from 'react'
import { Box, Typography } from '@mui/material'

const UnknownGroupTab = (props) => {

    return (
        <Box >
            <Typography variant='h4'>Неизвестная группа</Typography>
        </Box>
    )
}

export default UnknownGroupTab
